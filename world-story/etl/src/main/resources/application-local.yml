server:
  port: 8010

spring:
  application:
    name: etl

# 暂时禁用Nacos以便本地测试
# cloud:
#   nacos:
#     discovery:
#       enabled: false
#     config:
#       enabled: false

# ETL配置
etl:
  rest-port: 8081

# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
